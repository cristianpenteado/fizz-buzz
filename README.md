# FizzBuzz

**TODO: O projeto consiste em dada lista de números em um arquivo .txt sejam substituídos os números
múltipos de 3 por `Fizz`, os múltiplos de 5 por `Buzz` e os múltiplos de 3 e 5 por `FizzBuzz`**

## Comandos
- Para criar um projeto **Elixir** usamos o comando: `mix new nome-do-projeto`
- Para compilar basta rodar dentro da pasta do projeto:`mix compile`
- Para rodar os testes: `mix test`
